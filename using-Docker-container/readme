this is the same project , some changes are made to files and some new files are created and here we are doing this project using docker container , 
A docker container is a standard unit of software that packages up code and all its dependencies so the application runs quickly .
